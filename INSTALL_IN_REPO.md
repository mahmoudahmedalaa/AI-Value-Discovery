# Install into repository

1. Clone or open `mahmoudahmedalaa/AI-Value-Discovery`.
2. Unzip this pack into the repository root.
3. Review `CODEX_KICKOFF_PROMPT.md`.
4. Give that prompt to Codex.
5. Codex should commit and push the documentation/governance setup before application code.

Suggested commands:

```bash
unzip ai_value_discovery_codex_pack.zip -d AI-Value-Discovery
cd AI-Value-Discovery
git status
git add .
git commit -m "docs: add AI Value Discovery product build pack"
git push
```
