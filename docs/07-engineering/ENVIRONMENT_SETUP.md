# Environment Setup

Local requirements: Node LTS, package manager, Docker Desktop, GitHub CLI, optional Python, optional Ollama.

Services: Postgres, Qdrant, MinIO, web app, optional API/worker.

Example env: APP_ENV=local, AI_PROVIDER=mock, DATABASE_URL, QDRANT_URL, OBJECT_STORAGE_PROVIDER=minio, MINIO endpoint/keys.

Seed data: sample GCC bank workspace, users, docs, opportunities, scores, audit events.
