# Definition of Done

Feature is done only when it works locally, is visually consistent, handles loading/empty/error states, validates inputs, respects workspace/tenant boundaries, creates required audit events, has tests or documented reason for omission, updates docs, updates GitHub board, is committed and pushed.

AI feature done: provider gateway, schema validation, failure handling, evidence/assumption/inference separation, policy enforcement, mock mode.
